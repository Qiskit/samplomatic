`AddInjectNoise`, and hence `generate_boxing_pass_manager`, now always annotates boxes containing a measurement or a reset with the `"before"` inject noise site, regardless of the requested `site`. Virtual Paulis cannot travel leftward across a measurement or a reset, so an injection placed after the hard content of such a box was silently discarded on the qubits those instructions act on, while its sign was still reported.
