Fixed imports to not assume that the `typing` subpackage exists in the `numpy` namespace; the line `import numpy; numpy.typing` fails in some environments, whereas `import numpy.typing; numpy.typing` always succeeds under the current version restrictions.
