The `ChangeBasis` annotation now accepts `dressing` and `mode` as independent arguments. Peviously, a `mode` of `prepare` would result in a `right` dressing, while a `measure` would result in a `left` dressing.
