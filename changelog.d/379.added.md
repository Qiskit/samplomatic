Added a `history` option to `InjectNoise` (and `inject_noise_history` to `generate_boxing_pass_manager`) that records which Pauli-Lindblad generators were sampled at each injection site, exposed as a boolean `pauli_history.<ref>` samplex output.
