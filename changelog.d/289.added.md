Added the `num_bytes` attribute to `Specification`, `TensorSpecification`, and `PauliLindbladMapSpecification`, and the `num_bytes()` method to `TensorInterface`, to estimate memory usage for interface data.
