Added the `NoiseSource` protocol and `StaticNoiseSource`, an implementation of the given protocol.
Added the `Samplex.set_noise_source()` method to set the noise source to use for noise injection at sample time.
