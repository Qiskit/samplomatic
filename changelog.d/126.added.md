Added the `NoiseOracle` protocol and `StaticNoiseOracle`, an implementation of the given protocol.
Added the `Samplex.set_noise_oracle()` method to set the noise oracle to use for noise injection at sample time.
